<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="noticeMapper">
<insert id="add">
	insert into tbl_notice(nno, ncontent, nwriter, nimg, nfile)
	values (seq_notice.nextval,#{content},#{writer},#{img},#{file})
</insert>

</mapper>